const styles = theme => ({
  autoScalingText: {
    padding: '0 30px',
    position: 'absolute',
    right: 0,
    transformOrigin: 'right',
    display: 'inline-block',
  },
});

export default styles;
